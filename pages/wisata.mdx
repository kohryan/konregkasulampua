---
title: "Info Wisata"
description: Informasi terkait destinasi wisata dan pengetahuan tentang budaya & adat istiadat
type: posts
---

import { getPagesUnderRoute } from "nextra/context";
import Link from "next/link";
import Image from "next/image";

export function PariwisataHeader() {
  return (
    <div className="mx-auto mb-4 max-w-screen-lg border-b border-gray-400 border-opacity-20 pt-2 pb-4">
        <h1>
          <p className="font-bold text-center mb-3 lg:text-4xl">Informasi Wisata</p>
        </h1>
        <p className="text-center text-gray-500 dark:text-gray-400 font-space-grotesk">
          Klik salah satu banner di bawah ini untuk informasi selengkapnya
        </p>
    </div>
  );
}

export function BannerWisata() {
  return (
    <div className="mx-auto mb-6 max-w-screen-lg border-b border-gray-400 border-opacity-20">
      <div className="mb-2 grid grid-cols-3 gap-y-10 gap-x-2 xl:gap-x-2">
        <div className="text-center col-span-2">
          <Link href="/wisata/infowisata"><Image src="/pariwisata/banner-dispar.png" width="825" height="412" alt="dwp" className="inline-flex" /></Link>
        </div>
        <div className="text-center">
          <Link href="/wisata/pusat-oleholeh"><Image src="/pariwisata/banner-oleh.png" width="375" height="412" alt="kuliner" className="inline-flex" /> </Link>
        </div>
      </div>
      <div className="text-center mb-2">
        <Link href="/wisata/umkm"><Image src="/pariwisata/banner-umkm.png" width="1100" height="412" alt="umkm" className="inline-flex" /> </Link>
      </div>
      <div className="mb-6 grid grid-cols-2 gap-y-10 gap-x-2 xl:gap-x-2">
        <div className="text-center">
          <Image src="/pariwisata/banner-dwp.png" width="550" height="412" alt="dwp" className="inline-flex" />
        </div>
        <div className="text-center">
          <Link href="/wisata/kuliner"><Image src="/pariwisata/kuliner.png" width="550" height="412" alt="kuliner" className="inline-flex" /> </Link>
        </div>
      </div>
    </div>
  );
}

export function PariwisataIndex() {
  return getPagesUnderRoute("/wisata").map((page) => {
    return (
      <div className="flex items-center justify-center align-top">
        <div key={page.route} className="relative">
          <Link href={page.route}>
            <img
              className="items-center rounded-t-lg"
              src={page.frontMatter?.symbol}
              alt="icon"
            />
          </Link>
          <Link
            href={page.route}
            style={{ color: "inherit", textDecoration: "none" }}
            className="text-md mt-2 block text-center justify-center font-medium"
          >
            {page.meta?.title || page.frontMatter?.title || page.name}
          </Link>
        </div>
      </div>
    );
  });
}


<PariwisataHeader />
<BannerWisata />