---
title: "Info Akomodasi"
description: Informasi terkait hotel/penginapan, tempat makan, minimarket/swalayan, akses transportasi, dan fasilitas akomodasi lainnya yang terdekat/di sekitar lokasi kegiatan
type: posts
---

import { getPagesUnderRoute } from "nextra/context";
import Link from "next/link";

export function AkomodasiHeader() {
  return (
    <div className="max-w-screen-lg mx-auto pt-4 pb-8 mb-6 border-b border-gray-400 border-opacity-20">
      <h1>
        <p className="font-bold text-center mb-6 lg:text-5xl">Informasi Akomodasi</p>
      </h1>
      <p className="text-center text-gray-500 dark:text-gray-400 font-space-grotesk">
        Informasi terkait hotel/penginapan, tempat makan, minimarket/swalayan, akses transportasi, dan fasilitas akomodasi lainnya di sekitar lokasi kegiatan
      </p>
    </div>
  );
}

export function AkomodasiIndex() {
  return getPagesUnderRoute("/akomodasi").map((page) => {
    return (
        <div className="flex items-center justify-center align-top">
          <div key={page.route} className="relative">
            <Link href={page.route}>
              <img className="items-center rounded-t-lg" src={page.frontMatter?.symbol} alt="icon"/>
            </Link>
            <Link href={page.route} style={{ color: "inherit", textDecoration: "none" }} className="block font-medium mt-2 text-md text-center">
              {page.meta?.title || page.frontMatter?.title || page.name}
            </Link>
          </div>
        </div>
    );
  });
}

export function AkomodasiLayout() {
    return (
      <div className="max-w-2xl mx-auto px-4 sm:px-6 lg:max-w-7xl lg:px-8">
          <p className="text-center text-sm font-light text-gray-600 dark:text-white font-space-grotesk">
             Klik icon berikut untuk informasi selengkapnya
          </p>
        <div className="grid grid-cols-2 gap-y-10 mt-12 sm:grid-cols-2 gap-x-6 lg:grid-cols-4 xl:grid-cols-4 xl:gap-x-8 mx-auto mb-0">
              <AkomodasiIndex />
        </div>
      </div>
  );
}

<AkomodasiHeader />
<AkomodasiLayout />
